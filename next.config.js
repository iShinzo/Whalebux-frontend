/** @type {import('next').NextConfig} */
const nextConfig = {
  // Configuration for redirects
  async redirects() {
    // Example: Add meaningful redirects here in the future
    return [];
  },
};

module.exports = nextConfig;
